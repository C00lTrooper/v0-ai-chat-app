/**
 * Shared primary CTA styling (matches “Add task” on the Tasks tab).
 * Use with `<Button variant="default" size="sm" className={projectPrimaryButtonClassName} />`
 * or merge with `buttonVariants({ variant: "default", size: "sm" })` for Radix actions.
 */
export const projectPrimaryButtonClassName =
  "shrink-0 gap-1.5 rounded-lg px-3.5 py-2 text-sm font-semibold shadow-md ring-offset-background focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2";
